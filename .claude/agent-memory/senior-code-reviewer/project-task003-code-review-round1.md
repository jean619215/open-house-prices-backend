---
name: project-task003-code-review-round1
description: TASK-003 Code Review (commit a3820ed) — city_stats materialized view + GET /api/prices/city; passed round 1, no blockers, to QA測試.
metadata:
  type: project
---

Round 1 review of TASK-003 (map-city-prices-api), commit `a3820ed`. Scope: `alembic/versions/003_create_city_stats_view.py`, `etl/pipeline.py::_refresh_city_stats`/`run_pipeline`, `api/routers/prices.py`, `api/main.py`, plus new tests. **Verdict: ✅ 通過, card → QA測試.** No blockers found.

**What I verified beyond reading the tests' names (don't just trust test names/claims):**
- Actually ran `alembic upgrade 002:003 --sql` and `alembic downgrade 003:002 --sql` myself to regenerate the real DDL and confirm syntax (`CREATE MATERIALIZED VIEW city_stats AS ... WHERE city IS NOT NULL GROUP BY city WITH DATA`, `DROP MATERIALIZED VIEW city_stats`) — don't just take the developer's offline-SQL claim at face value, reproduce it.
- Ran `uv run ruff check --select D,ANN api/routers/prices.py` directly (not whole-project) to confirm the new file itself is clean, separately from whole-project `ruff check --select D api/` which does surface pre-existing D413/D104 in `health.py`/`__init__.py` — confirmed those are NOT introduced by this diff and `pyproject.toml`'s `[tool.ruff.lint].select` genuinely excludes `ANN`/`D` (`select = ["E", "F", "W", "I", "UP", "B", "C4", "PTH"]`). This is a recurring "known gap" the backend-engineer discloses on every card; it's real and not this card's problem each time — don't re-flag it as this task's blocker.

**Reusable SQL knowledge for this project's matview pattern:** `AVG(numeric)` over an all-NULL group returns `NULL` (not an error, not 0), and `ROUND(NULL, 2)` also returns `NULL` — this is standard PostgreSQL/SQL semantics, not something the codebase needs special-case handling for. So AC like "avg must be NULL not 0, must not raise divide-by-zero when a city has zero priced rows" is automatically satisfied by plain `ROUND(AVG(price_per_sqm), 2)` with no `CASE`/`NULLIF` needed. Don't ask the dev to add defensive NULL-guard SQL here — it's already correct.

**Refresh pattern established in this codebase (etl/pipeline.py):** `_refresh_city_stats(session)` runs `REFRESH MATERIALIZED VIEW city_stats` (non-CONCURRENTLY) via `text()` on the *same* session that `load_rows` just used, with its own separate `session.commit()` after `load_rows` already committed once. This is safe: SQLAlchemy AsyncSession autobegins a new transaction after a commit, so the REFRESH's implicit transaction is independent. A unique index (`ix_city_stats_city`) was added proactively even though refresh isn't `CONCURRENTLY` yet — that's forward-looking design for a later perf task, not dead code.

**Environment-constraint precedent (carries over from [[project-task002-code-review-round2]]):** this project's dev/QA environment has no working docker daemon, so DB-dependent migration/REFRESH test cases (here: TC-01/02/07/08/09) get verified via `alembic ... --sql` (offline DDL generation) + mocked pytest instead of a real PostgreSQL run. I judged this **acceptable to pass Code Review, not a blocker**, when: (a) the SQL involved is standard/well-known (plain matview + GROUP BY + AVG/ROUND, not exotic PostGIS or recursive CTEs), (b) the offline SQL was actually regenerated and inspected rather than trusted, and (c) mock-level unit tests cover the surrounding orchestration logic (refresh called/not-called, exception propagation). I flagged it as a required QA測試-stage follow-up (verify TC-01/02/07/08/09 against real Postgres if an environment becomes available) rather than bouncing the card — bouncing over an environment limitation the team already has no fix for just burns a rejection count for no code-quality gain. If a *future* task's DB-only-verifiable logic involves PostGIS spatial correctness, non-trivial aggregation, or migration `downgrade` data loss, raise the bar — offline SQL syntax checking alone would not be enough there.

**Process note:** Round 1, no rejection. Card history for TASK-003 already logs prior rounds' conventions faithfully (QA writes AC/TC → dev implements → senior review); this round continued the pattern with zero Majors/Blockers, first clean pass for a card in this project so far (TASK-001 and TASK-002 both bounced at least once).
